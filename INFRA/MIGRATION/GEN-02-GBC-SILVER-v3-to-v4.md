# GEN-02 / GBC / SILVER → repository v4

Canonical library: `LIBRARY/GEN-02/GBC/SILVER/`  
Canonical project: `PROJECTS/SILVER-MODERNIZATION/`

v4 separates official release identity (`<MARKET>-<LANGUAGE>-HV<HEADER-VERSION>`) from exact supplied dump identity (`PROJECT-<SHA1-prefix>`). Observed `AAX*` header bytes remain metadata rather than GB/GBC canonical release keys. Hashes were recalculated directly from the currently supplied source files. Temporary v3 Silver manifests are removed instead of retained as duplicate live identities.
