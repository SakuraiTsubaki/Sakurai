# LeafGreen v6 path contract

Canonical source identity: `LIBRARY/GEN-03/LEAFGREEN/SOURCE/GBA/CART/<RELEASE-ID>/`.

Current release IDs: `BPGJ-HV0`, `BPGE-HV0`, `BPGE-HV1`, `BPGD-HV0`, `BPGF-HV0`, `BPGI-HV0`, `BPGS-HV0`.

Exact uploads are dump observations under `DUMPS/<DUMP-ID>/`; same-game comparisons live under `LIBRARY/GEN-03/LEAFGREEN/COMPARE/<COMPARISON-ID>/`.

Sakurai owns identity, hashes, provenance, native structure, pointer/bank/page observations, semantic research, disassembly, bugs/unused research and verification. Tsubaki uses the same IDs and owns production manifests, extracted/normalized assets, dedup catalogs, conversions, patches, builds and implementation resources.

Projects are generation-scoped: `PROJECTS/GEN-03/LEAFGREEN-MODERNIZATION/`, `PROJECTS/GEN-03/LEAFGREEN-PAST-PARADOX-001-386/`, `PROJECTS/GEN-03/LEAFGREEN-BW-DIRECT-PARAMS/`.

Raw `.gba` files and raw copyrighted payload dumps are never committed. `R0/R1` aliases are legacy labels; v6 canonical GBA release identity uses `HV0/HV1`.
