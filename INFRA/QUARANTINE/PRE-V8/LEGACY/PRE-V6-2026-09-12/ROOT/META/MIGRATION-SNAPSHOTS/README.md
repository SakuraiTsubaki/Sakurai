# Migration snapshots

`V1/` preserves the former Roman-generation hierarchy. `V2/` preserves the intermediate standalone `GEN-XX` model. Both are read-only audit sources; active work belongs under `GAMES/` using v3.
