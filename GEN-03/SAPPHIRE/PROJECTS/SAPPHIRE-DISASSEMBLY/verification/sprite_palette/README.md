# Sprite palette verification

Expected per-function SHA-256 fingerprints for the eight-function palette-management block mapped in `config/sprite_palette.yml`.

Use:

```sh
make analyze-sprite-palette ROM=/path/to/reference.gba
```

ROM binaries are not stored in this repository.
