# Sprite sheet/tile-range verification

Expected per-function SHA-256 fingerprints for the 14-function `sprite.c` block mapped in `config/sprite_sheet_tiles.yml`.

Use:

```sh
make analyze-sprite-sheet-tiles ROM=/path/to/reference.gba
```

ROM binaries are not stored in this repository.
