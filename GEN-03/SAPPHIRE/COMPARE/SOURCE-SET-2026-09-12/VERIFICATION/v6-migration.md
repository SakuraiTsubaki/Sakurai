# Sapphire v6 migration verification

Canonical source identities use `<GAME-CODE>-HV<HEADER-VERSION>` and exact observed dumps use `UPLOAD-<SHA1-8>`.

Nine source/dump pairs are registered. The previous `AXP?-R?` and `PROJECT-*` names remain legacy aliases only. Hashes and observed filenames are unchanged. Original ROM binaries are not committed.
