# Silver verification

All eight supplied Silver ROM files are local read-only inputs. SHA-1, SHA-256, MD5, size, header metadata, header checksum and global checksum are recorded per dump. Both Game Boy checksums validate for every supplied ROM. ROM images, patched ROM images, and concatenable lossless bank-chunk mirrors are not committed.
