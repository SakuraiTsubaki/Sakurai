# DESIGN

Design documents are derived project work, not official-release facts.

Canonical subdomains: `ENGINE`, `DATA-MODEL`, `ROM-EXPANSION`, `POINTERS`, `SAVE-EXPANSION`, `GRAPHICS-CONVERSION`, `AUDIO-CONVERSION`, `SCRIPTING`, `COMPATIBILITY`, `UI`, `COMMUNICATION`.

A design may cite one or more `LIBRARY` releases, but must not duplicate release identity or dump provenance as project-owned facts. Where target engine limits conflict with Generation V content, expand the engine/data model before considering content reduction.