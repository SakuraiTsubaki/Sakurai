# Implementation design

This directory owns research-side design for Hangul encoding/decoding, font addressing, text boxes, line/page control, name/SRAM strings, menu layout, pointer relocation, bank allocation, and target-specific text placement.

Existing source offsets are observations, not translation length constraints. Target implementations may relocate text and update pointers/bank selectors as needed.
